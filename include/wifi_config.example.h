#pragma once

// Template. Copy to wifi_config.h (gitignored) and fill in real credentials.

static constexpr const char* WIFI_SSID     = "your-ssid";
static constexpr const char* WIFI_PASSWORD = "your-password";
